import { LazyscrollDirective } from './lazyscroll.directive';

describe('LazyscrollDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyscrollDirective();
    expect(directive).toBeTruthy();
  });
});
