import { WishitemDirective } from './wishitem.directive';

describe('WishitemDirective', () => {
  it('should create an instance', () => {
    const directive = new WishitemDirective();
    expect(directive).toBeTruthy();
  });
});
